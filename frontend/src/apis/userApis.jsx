const USER_BASE_URL = "/user";

export const USER_API = {
  REGISTER: `${USER_BASE_URL}/register`,
  LOGIN: `${USER_BASE_URL}/login`,
  FETCH_USER: `${USER_BASE_URL}/infor`,
  LOGOUT: `${USER_BASE_URL}/logout`,
};
